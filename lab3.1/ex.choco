p [Enter your name:]
name ~ i
[Bill]:name ? p (name + [ is a good name])

p [Guess a color]
color ~ [red]
guess ~ i
while guess:color & color:guess do {
  guess < color ? {
    p [Your guess is less than the color]
  } ; guess > color ? {
    p [Your guess is greater than my color]
  } 

  r ~ (guess:color & color:guess) ~ ? guess~i
  }
p [You guessed correct!]
